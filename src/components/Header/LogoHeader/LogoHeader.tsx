import styled from "styled-components";

const LogoContainer = styled.div`
  display: flex;
  align-items: center;
  font-size: 40px;
`;

const LogoHeader = () => {
  return <LogoContainer>CCONVERT</LogoContainer>;
};

export default LogoHeader;
